pipeline {
  agent any
  stages {
    stage('first') {
      steps {
        script {
          sh label: 'Build Docker Image', script: """docker built . -t chihchinag/test"""
        }
      }
    }
  }
}
