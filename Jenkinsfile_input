pipeline {
  agent any
  parameters {
    input message: 'https://abcde', parameters: [choice(choices: ['MAC', 'Linux', 'Windows'], description: 'OS platform', name: 'pppp')]
  }
  
  stages {
    stage('Hello') {
      steps {
      	script{
          sh label: '', script: 'env'
	  echo "Hello, ${params.pppp}, nice to meet you."
	}
      }
    }
  }
}
