pipeline {
  agent any
  
  stages {
    stage('Hello') {
      steps {
      	script{
	  PLATFORM = input message: 'https://abcde', parameters: [choice(choices: ['MAC', 'Linux', 'Windows'], description: 'OS platform', name: 'platform')]
          sh label: '', script: 'env'
	  echo "Hello, ${PLATFORM}, nice to meet you."
	}
      }
    }
    
    stage('MAC') {
      when {
        expression { PLATFORM == 'MAC' }
      }
      steps {
      	script{
	  echo "Welcome ${platform}, nice to meet you."
	}
      }
    }
    
  }
}
